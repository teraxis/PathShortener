# Claude Code Instructions

Follow `docs/agent-playbook.md` as the shared source of truth.

Work directly in `D:\Projects\PathShortener` by default. Do not create worktrees, duplicate project copies, or switch branches unless the user explicitly asks for that in the current task.

If `PathShortener_SKILL.md` is present, read it before changing project behavior. Do not modify unrelated files, and run the relevant verification commands before reporting completion.
